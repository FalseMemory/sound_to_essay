# ============================================================
# 依赖与构建产物
# ============================================================
node_modules/
dist/
dist-mobile/
.vite-check/
.vite-check*
.vite-build-tmp/
target/
python_stt/.venv/
python_stt/__pycache__/
__pycache__/
*.pyc
.vite/
*.local

# ============================================================
# 个人数据 —— 绝不入库
# ------------------------------------------------------------
# data/   真实口述史数据库（sound_to_essay.sqlite3 及其副本）
# sounds/ 真实录音
# models/ 语音识别模型（体积大，且属本地环境）
# 这些是用户的实际资料，不是项目代码。
# ============================================================
data/
sounds/
models/

# ============================================================
# 本地自签证书 —— 绝不入库
# 含私钥（*.key），且只对本机局域网有意义。
# 需要时用 tools/make-dev-cert.ps1 重新生成。
# ============================================================
certs/

# 本地 CA 证书的发布副本（由 tools/make-dev-cert.ps1 生成，只对本机局域网有意义）
public/sound-to-essay-ca.crt

# ============================================================
# 发布产物
# ============================================================
shengwen-*.zip
release-candidate-*/

# ============================================================
# 工作区记忆与助手状态（含内部讨论记录，不随代码公开）
# ============================================================
.workbuddy/

# ============================================================
# 杂项
# ============================================================
*.log

# 开发过程中的临时文件（如诊断脚本的中间输出），避免误入库
_*.txt
.DS_Store
Thumbs.db
